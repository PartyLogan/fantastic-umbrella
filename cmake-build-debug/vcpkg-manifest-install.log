Detecting compiler hash for triplet x64-linux...
All requested packages are currently installed.
Restored 0 packages from /home/logan/.cache/vcpkg/archives in 696 ns. Use --debug to see more details.

Total elapsed time: 471 ms

The package glad provides CMake targets:

    find_package(glad CONFIG REQUIRED)
    target_link_libraries(main PRIVATE glad::glad)

The package sdl2 provides CMake targets:

    find_package(SDL2 CONFIG REQUIRED)
    target_link_libraries(main PRIVATE SDL2::SDL2 SDL2::SDL2main SDL2::SDL2-static)

The package stb is header only and can be used from CMake via:

    find_path(STB_INCLUDE_DIRS "stb.h")
    target_include_directories(main PRIVATE ${STB_INCLUDE_DIRS})

The package utf8proc provides CMake targets:

    find_package(unofficial-utf8proc CONFIG REQUIRED)
    target_link_libraries(main PRIVATE utf8proc)

The package zlib is compatible with built-in CMake targets:

    find_package(ZLIB REQUIRED)
    target_link_libraries(main PRIVATE ZLIB::ZLIB)

